# Environment variables — never commit these
.env.local
.env

# macOS
.DS_Store

# Vercel
.vercel

# Claude Code
.claude/

# Supabase CLI local cache/session state — churns on every CLI invocation
supabase/.temp/

# Business documents
*.pdf

# Private chef variant (managed separately)
haccp-app PC.html

# One-shot broadcast tooling — contains PII (contact emails) and hardcoded API keys
emails/
